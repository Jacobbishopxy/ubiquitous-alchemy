
baseUrl=http://localhost:8080
baseLoginPath=/login/cas
baseLogoutPath=/logout/cas

casUrl=https://cas.example.com
casLoginUrl=https://cas.example.com/login
casLogoutUrl=https://cas.example.com/logout
casKey=CAS_PROVIDER_LOCALHOST
casTimeout=43200

appUser=appUser
appKey=appKey
appInfo=dev-test
authenticateUrl=https://cas.example.com/authenticate
validateUrl=https://cas.example.com/validate
